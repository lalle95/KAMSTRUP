pipeline {
    agent {label 'slave'}
    stages {
        stage('Back-end') {
            steps {
                sh 'pwd'
            }
        }
        stage('Front-end') {
            steps {
                sh 'ls'
            }
        }
    }
}