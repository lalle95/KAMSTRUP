pipeline {
    agent {label 'slave'}
    stages {
        stage('build') {
            steps {
                sh 'pwd'    
            }
        }
        stage('deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}
