pipeline {

    agent any

    //agent {
    //    docker {image 'busybox:latest'} 
    //}
  
    stages {

        stage('build') {
            steps {
                echo 'build'
            }
        }

        stage('test') {
            steps {
                echo 'test'
            }
        }

        stage('deploy') {
            steps {
                echo 'deploy'
            }
        }
    }
}